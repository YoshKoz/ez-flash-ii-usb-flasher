[package]
name = "ezwriter-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
rusb = { version = "0.9", features = ["vendored"] }
anyhow = "1.0"
clap = { version = "4", features = ["derive"] }
hex = "0.4"
