[package]
name = "ezwriter-cli"
version = "0.1.0"
edition = "2024"
description = "Command-line tool for the EZ-Flash II / EZ-Writer II USB GBA flasher"

[dependencies]
rusb = { version = "0.9", features = ["vendored"] }
anyhow = "1.0"
clap = { version = "4", features = ["derive"] }

