[package]
name = "ezwriter-gui"
version = "0.1.2"
edition = "2024"
description = "GUI for the EZ-Flash II / EZ-Writer II USB GBA flasher"

[dependencies]
eframe = { version = "0.36", features = ["default"] }
rusb = { version = "0.9", features = ["vendored"] }
anyhow = "1.0"
rfd = "0.17"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# rebuild
